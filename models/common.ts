export type MetaType = {
  page: number;
  pageSize: number;
  total: number;
  pages?: number;
};

export type PreviewType = {
  id: string;
  path: string;
  preview: string;
  originalName: string;
};
